Frontend — Virtual AI Presenter

Run locally (frontend only):

```bash
cd virtual-ai-presenter/frontend
npm install
npm run dev
```

The frontend expects the backend mock to be running on `http://localhost:8000` (see `mvp/backend`). Use `npm run build` to create production assets.
